<Window x:Class="Eshop.UI.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Prihlaseni do MySQL" Height="353" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Host:"/>
            <TextBox x:Name="HostBox" Text="localhost"/>

            <TextBlock Text="Databaze:" Margin="0,5,0,0"/>
            <TextBox x:Name="DbBox" Text="eshop"/>

            <TextBlock Text="Uzivatel:" Margin="0,5,0,0"/>
            <TextBox x:Name="UserBox"/>

            <TextBlock Text="Heslo:" Margin="0,5,0,0"/>
            <PasswordBox x:Name="PasswordBox"/>
        </StackPanel>

        <Button Grid.Row="3" Content="Prihlasit"
                Height="35" Margin="0,5,0,0"
                Click="Login_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform Y="-15"/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

        <Button Content="Nacist konfiguraci ze souboru"
            Margin="0,53,0,-10"
            Click="LoadConfig_Click" Height="30" Grid.Row="3"/>
    </Grid>
</Window>
